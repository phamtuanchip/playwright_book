name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install demo-app dependencies
        working-directory: code/demo-app
        run: npm ci

      - name: Install playwright-tests dependencies
        working-directory: code/playwright-tests
        run: npm ci

      # Từ Chương 24: ch24-cross-browser-mobile.spec.ts chạy trên cả 4 project (chromium,
      # firefox, webkit, mobile-chrome) — cần cài đủ cả 3 engine, không chỉ Chromium như trước.
      - name: Install Playwright browsers (Chromium, Firefox, WebKit)
        working-directory: code/playwright-tests
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Run Playwright tests
        working-directory: code/playwright-tests
        run: npm test

      - name: Upload HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: code/playwright-tests/playwright-report/
          retention-days: 7
